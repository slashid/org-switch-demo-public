import { style } from "@vanilla-extract/css";

export const root = style({
  display: "grid",
  placeItems: "center",
  width: "100%",
  height: "100%"
});

export const content = style({
  width: "465px"
});
